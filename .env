# Variáveis de Ambiente para o Prisma e NextAuth
# Copie este arquivo para .env.local e preencha os valores.
# NUNCA envie o arquivo .env.local para o repositório.

# URL de conexão completa para o seu banco de dados PostgreSQL.
# Exemplo: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:password@localhost:5432/mobiceolin"

# Um segredo usado pelo NextAuth para criptografar os JWTs.
# Gere um valor aleatório forte para isso.
# Comando para gerar um segredo no Linux/Mac: openssl rand -base64 32
AUTH_SECRET=

# Chave de API para o Genkit (se estiver usando funcionalidades de IA)
GEMINI_API_KEY=
